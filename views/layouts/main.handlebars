<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Some Tech Blog</title>

    {{!-- style sheets go here  --}}
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    
</body>

    <div>
        {{!-- header which will contain log in and log out as well as main page header  --}}

        <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <h1><a class="navbar-brand" href="/">Some Tech Blog</a></h1>
                <div class="navbar-nav">
                    <a class="nav-link" href="/">Home</a>
                    {{#if loggedIn}}
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                    <button class="btn btn-secondary" id="logout">Logout</button>
                    {{else}}
                    <a class="btn btn-secondary" href="/login">Login</a>
                    {{/if}}
                </div>
            </nav>
        </header>

        <main>
            {{{body}}}
        </main>

        <footer>
            Ya'll come back now.
        </footer>

    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</html>